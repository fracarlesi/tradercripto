services:
  bot:
    build: .
    container_name: hlquantbot_bot
    command: ["python", "-m", "simple_bot.main"]
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
